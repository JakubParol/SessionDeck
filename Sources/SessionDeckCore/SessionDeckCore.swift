// SessionDeckCore exposes Domain, Application, Infrastructure, and composition-root
// types from folder-level source files. Keep this file intentionally empty so the
// module entry point stays lightweight while SwiftPM compiles the layer folders.
